@model IEnumerable<Prueba.ANO_LECTIVO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ANO_DESCRIP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ANO_DESCRIP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_ANO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_ANO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_ANO })
        </td>
    </tr>
}

</table>
