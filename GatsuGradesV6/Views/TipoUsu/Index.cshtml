@model IEnumerable<GatsuGradesV6.Models.TipoUsuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TuDescrip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TuDescrip)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipou">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipou">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipou">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
